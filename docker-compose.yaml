version: "3.3"

services:
  mysql: 
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'technical'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'secret'
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - ./mysql-data:/var/lib/mysql
    networks:
      - test-net
networks:
  test-net:
    driver: bridge
